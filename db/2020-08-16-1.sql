CREATE TABLE IF NOT EXISTS  `channels` (
    `id` text NOT NULL,
    `brandingSettings` text,
    `contentDetails` text,
    `contentOwnerDetails` text,
    `invideoPromotion` text,
    `localizations` text,
    `snippet` text NOT NULL,
    `status` text,
    `topicDetails` text
)